export interface Experience {
  icon: string;
  title: string;
  description: string;
  date: string;
}

export interface Project {
  title: string;
  description: string;
  link: string;
  icon: string;
}
